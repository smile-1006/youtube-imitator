import React from 'react';
import ReactDOM from "react-dom/client";
import "./index.css"
import App from "./App"

const root = ReactDOM.createRoot(document.getElementById("root"));
// root.render for rendering the root Id from "public/index.html"
root.render(<App />)